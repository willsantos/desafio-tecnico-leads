/** Values accepted by `personalDocumentSubtype` (README seção 5 rota POST .../documents),
 * mirrors identification's `documentType` — a document sent here should match etapa 3's type. */
export const PERSONAL_DOCUMENT_SUBTYPES = ['CNH', 'RG'] as const
